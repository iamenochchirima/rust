fn find(nums: Vec<i32>, target: i32,) -> i32 {
    let first = 0;
    let last = nums.len() -1;
    
    // while first <= last {
    //     mid = (first + last) / 2;
    //     if nums[mid] == target {
    //         return mid
    //     }
    //     if 
    // }
}